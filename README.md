# ProblemMediaTypeIssue

Basic project to demonstrate that using the `Produces` attribute to a controller stops it from returning a problem response with `media type` `application/problem+json` as it should.
